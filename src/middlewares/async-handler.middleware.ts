/**
 * Async Handler Middleware
 * ========================
 * In Elysia, async handlers are supported natively.
 * This file is kept for compatibility/reference.
 */

export const asyncHandler = (fn: Function) => fn;